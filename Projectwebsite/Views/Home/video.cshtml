<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Video Showcase</title>
    <style>
        /* 星空背景 */
        body {
            margin: 0;
            padding: 0;
            font-family: "Arial", sans-serif;
            color: #fff;
            text-align: center;
            overflow: hidden;
            background: linear-gradient(45deg, #0d1b2a, #1b263b, #415a77, #778da9);
            background-size: 400% 400%;
            animation: gradientBG 10s ease infinite;
        }

        /* 背景动画 */
        @@keyframes gradientBG {
            0%

        {
            background-position: 0% 50%;
        }

        50% {
            background-position: 100% 50%;
        }

        100% {
            background-position: 0% 50%;
        }

        }

        /* 星空粒子特效 */
        .stars {
            position: fixed;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            background: url('https://cdn.pixabay.com/photo/2016/11/19/14/00/star-1837306_1280.jpg') repeat;
            opacity: 0.2;
            animation: starsMove 30s linear infinite;
        }

        @@keyframes starsMove {
            from

        {
            background-position: 0 0;
        }

        to {
            background-position: -1000px 1000px;
        }

        }

        /* 炫彩标题 */
        h1 {
            font-size: 3rem;
            font-weight: bold;
            background: linear-gradient(90deg, #f0f, #ff0, #0ff, #f0f);
            -webkit-background-clip: text;
            color: transparent;
            animation: glow 2s infinite alternate;
        }

        @@keyframes glow {
            from

        {
            text-shadow: 0 0 5px #fff;
        }

        to {
            text-shadow: 0 0 20px #ff0, 0 0 30px #f0f;
        }

        }

        /* 视频容器 */
        .video-container {
            margin-top: 30px;
            display: flex;
            justify-content: center;
        }

        /* 视频盒子 */
        .video-box {
            background: rgba(255, 255, 255, 0.1);
            padding: 15px;
            border-radius: 15px;
            backdrop-filter: blur(5px);
            transition: all 0.5s ease;
        }

            .video-box:hover {
                transform: scale(1.05);
                box-shadow: 0px 0px 20px rgba(255, 255, 255, 0.5);
            }

        /* 返回主页按钮 */
        .btn {
            display: inline-block;
            margin-top: 30px;
            padding: 15px 30px;
            font-size: 1.2rem;
            color: #fff;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 25px;
            text-decoration: none;
            transition: all 0.3s ease;
        }

            .btn:hover {
                background: rgba(255, 255, 255, 0.5);
                transform: scale(1.1);
            }
    </style>
</head>
<body>

    <!-- 星空背景 -->
    <div class="stars"></div>

    <h1>Video Showcase</h1>

    <div class="video-container">
        <div class="video-box">
            <iframe width="800" height="450"
                    src="https://www.youtube.com/embed/PUvxHzRoGJg"
                    title="YouTube video"
                    frameborder="0"
                    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                    allowfullscreen>
            </iframe>
        </div>
    </div>

    <a class="btn" href="@Url.Action("Index", "Home")">🏠 Home</a>



</body>
</html>
